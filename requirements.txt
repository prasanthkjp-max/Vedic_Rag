# Vedic Astrology RAG Portal — pinned to the versions verified on the Pi.
# Regenerate after upgrades with: pip freeze > requirements.txt

# Web backend
fastapi==0.135.3
uvicorn==0.44.0
pydantic==2.12.5

# PDF generation + complex-script (Tamil/Indic) text shaping
reportlab==4.5.1
uharfbuzz==0.54.1

# OCR ingestion pipeline
PyMuPDF==1.27.2.3
pillow==11.1.0
pytesseract==0.3.13

# Astronomy / ephemeris (sidereal positions, ayanamsa, rise/set) — imported as
# `swisseph` by astro_engine.py
pyswisseph==2.10.3.2

# Vector search
numpy==2.2.6

# LLM chat + RAG embeddings via OpenRouter (OpenAI-compatible API)
openai==1.59.6

# Payments — Razorpay Orders API (UPI/cards). Optional at runtime: imported
# lazily so the app still starts/CI still passes when payments are turned off.
razorpay==1.4.2

# Note: pytesseract requires the system 'tesseract-ocr' binary plus the
# 'san' (Sanskrit) and 'eng' language packs installed via the OS package manager.
# The RAG chat and embeddings require an OPENROUTER_API_KEY (see config.py / .env.example).
