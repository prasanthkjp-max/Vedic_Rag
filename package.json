{
  "name": "vedic-astro-ai",
  "version": "1.8.7",
  "private": true,
  "description": "Vedic Astro AI — cross-platform mobile wrapper via Capacitor",
  "scripts": {
    "cap:sync": "cap sync",
    "cap:android": "cap open android",
    "cap:build:android": "cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
